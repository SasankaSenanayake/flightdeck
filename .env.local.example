# Anthropic Admin API key (sk-ant-admin...) from console.anthropic.com
ANTHROPIC_ADMIN_KEY=

# Set to 1 to attempt the undocumented Google Cloud Code quota endpoint
ANTIGRAVITY_LIVE_QUOTA=0

# Weather widget (Today section). Free-text city, geocoded via Open-Meteo (no key needed).
WEATHER_LOCATION=

